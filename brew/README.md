Homebrew formulae and casks are listed in [`../profiles/tools.json`](../profiles/tools.json).

`./install.sh home` and `./install.sh work` render a Brewfile at runtime via [`../scripts/linux-tools.py`](../scripts/linux-tools.py). Do not add `*.Brewfile` files here.
