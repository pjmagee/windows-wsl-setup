# Home extras. install.sh home loads this after brew/universal.Brewfile.
# grok-build is xAI Grok Build. Do not brew install grok (unrelated regex tool).
# fluxcd (not flux) is Flux CD.

brew "opencode"
brew "changie"
brew "hugo"
brew "stripe-cli"

cask "claude-code"
cask "grok-build"
cask "devtunnel"
