# Extra steps for the work profile (on top of universal).
# Copilot CLI is in brew/work.Brewfile (install_brew already loaded it).
# Do not add grok, claude, opencode, devtunnel, changie, hugo, or stripe.
