[package]
name = "wwm"
version = "0.2.0"
edition = "2021"
description = "Windows WSL Manager — collect, restore, New WSL, and named software profiles"
license = "MIT"
publish = false

[build-dependencies]
winresource = "0.1"

[dependencies]
crossterm = "0.28"
ratatui = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(windows)'.dependencies]
winreg = "0.55"
windows-sys = { version = "0.59", features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
] }
